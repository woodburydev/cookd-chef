import React from 'react';
import { View, Text } from 'react-native';
import { commonStyles } from 'src/config/styles';

export default function InviteChef() {
  return (
    <View style={commonStyles.FlexColCenterCenter}>
      <Text>Invite A Chef</Text>
    </View>
  );
}
